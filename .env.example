# .env.example — Sample environment file for langchain-doc-qa-bot
# Copy this file to `.env` and fill in your actual API keys locally.
# Do NOT commit your `.env` file with real keys to GitHub.

# OpenAI or Ollama
OPENAI_API_KEY=your-openai-api-key-here
OPENAI_API_BASE=http://localhost:11434/v1

# Optional: Pinecone (if using Pinecone instead of FAISS)
PINECONE_API_KEY=your-pinecone-api-key-here
PINECONE_ENVIRONMENT=us-east1-gcp
PINECONE_INDEX=doc-qa-index

# Optional: HuggingFace token (if using HF models)
HUGGINGFACE_API_TOKEN=your-huggingface-token-here
